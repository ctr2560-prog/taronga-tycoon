{
  "name": "taronga-tycoon",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "esbuild src/game.ts src/data.ts --bundle --format=esm --outdir=dist-smoke --log-level=warning && node smoke.mjs",
    "demo": "npm run build && ./make-demo.sh"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
